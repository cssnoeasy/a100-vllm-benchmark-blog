---
title: "KV Cache显存分配不均排查：多卡推理的隐形杀手"
date: 2026-07-22
description: "从结构性不均到PP不对称——虽然本次Qwen2.5-7B部署双卡显存完美一致，但系统梳理社区两大根因及完整排查方法，助你提前规避"
categories: ["踩坑", "运维"]
---

# KV Cache显存分配不均排查：多卡推理的隐形杀手

> 硬件：双 A100 80GB PCIe（无 NVLink）· 模型：Qwen2.5-7B-Instruct · 推理框架：vLLM 0.19.0 · TP=2

## 一、引言

在多卡推理中，一个容易被忽视的事实是：**模型权重按 TP 均匀切分，不代表 KV Cache 也分配均匀**。vLLM 的张量并行将每一层的权重矩阵沿隐藏维度等分到各 GPU，权重的显存占用天然对称。但 KV Cache 的分配逻辑涉及注意力头（attention heads）的切分、层分配策略、以及 vLLM 内部的内存规划器——这其中任何一个环节的微小不对称，都可能导致某张卡的 KV Cache 池偏小，成为整个推理服务的瓶颈卡。

本文的特殊之处在于：**我在实际部署中并没有遇到这个问题**。Qwen2.5-7B（28 heads）+ TP=2 的组合是完美整除的——双卡显存始终稳定在 74003 MiB/74003 MiB。但"我没踩到"不代表"这个问题不存在"——vLLM 社区 GitHub Issues 中有大量分配不均的案例。本文系统梳理社区已知的两大根因场景及完整排查方法，帮你提前规避。

## 二、我的实测情况：幸运的"均匀"

在 Qwen2.5-7B + 双 A100 80GB + TP=2 的部署中，持续运行一周后 `nvidia-smi` 显示：

| GPU | 显存占用 | 总显存 | 利用率 | 状态 |
|:---|:---|:---|:---|:---|
| GPU 0 | 74,003 MiB | 81,920 MiB | 90.3% | 正常 |
| GPU 1 | 74,003 MiB | 81,920 MiB | 90.3% | 正常 |

双卡显存占用精确一致，且一周内未出现任何漂移。这得益于两点：Qwen2.5-7B 的 28 个 attention heads 可以被 TP=2 整除（28 ÷ 2 = 14），且使用了张量并行而非流水线并行（避免了层分配不对称问题）。

但这是"选对了参数"的结果，不是"问题不存在"的证据。下面两个根因场景，任何一条命中了，你的双卡显存就会立刻出现明显差距。

## 三、根因一：注意力头数无法被 TP 整除（结构性不均）

### 问题现象

最直接的表现是 vLLM 启动时直接报错：

```
ValueError: Total number of attention heads (28) must be divisible by tensor parallel size (8)
```

或者更隐蔽的情况——vLLM 通过 padding 方式强行运行，不报错但各卡 KV Cache 分配量不同，某张卡显存占用明显偏高。

### 根因分析

vLLM 的张量并行要求将注意力头均匀切分到各 GPU 上。内部有严格的整除检查：

```python
assert self.total_num_heads % tp_size == 0
assert self.total_num_kv_heads % tp_size == 0
```

如果 `num_attention_heads` 或 `num_kv_heads` 不能被 `tp_size` 整除，vLLM 要么直接拒绝启动（严格模式），要么通过补齐（padding）方式分配不等的 KV 头到各卡上，导致各 GPU 的 KV Cache 张量大小不同，显存占用出现结构性差异。

### 常见模型对照表

选择 TP 大小时，务必先查模型的 head 数：

| 模型 | num_attention_heads | num_kv_heads | 推荐 TP 值 | 不可用的 TP 值 |
|:---|:---|:---|:---|:---|
| Qwen2-7B | 28 | 4 | 1, 2, 4, 7, 14, 28 | 3, 5, 6, 8 |
| Qwen2.5-7B | 28 | 4 | 1, 2, 4, 7, 14, 28 | 3, 5, 6, 8 |
| Qwen2-72B | 64 | 8 | 1, 2, 4, 8, 16, 32, 64 | 3, 6, 12 |
| LLaMA 3.1-8B | 32 | 8 | 1, 2, 4, 8, 16, 32 | 3, 6, 12 |
| DeepSeek-V2 | 128 | 16 | 1, 2, 4, 8, 16, 32, 64, 128 | 3, 5, 6, 7 |

其中 **Qwen2-7B/2.5-7B 的 28 heads 是最容易踩坑的模型之一**——很多工程师习惯性地用 TP=8 去配 8 卡机器，但 28 不能被 8 整除，直接报错。本次实验用 TP=2（28 ÷ 2 = 14，完美整除），双卡显存一致，验证了正确选择 TP 即可从根源上避免此问题。

### 解决方案

**方案一（最直接）**：查模型 config，选可整除的 TP 值。

```bash
cat /path/to/model/config.json | grep -E "num_attention_heads|num_key_value_heads"
```

确保 `num_attention_heads % tensor_parallel_size == 0`。

**方案二**：如果业务需求确实需要不可整除的 TP 值，升级到 vLLM 0.12.0+，部分版本已通过 padding 方式支持非整除 head 分配（会牺牲少量显存效率）。

**方案三**：使用 `--enforce-eager` 绕过 CUDA Graph 的预分配检查。

```bash
vllm serve /path/to/model --tensor-parallel-size 8 --enforce-eager
```

此选项会禁用 CUDA Graph 加速，可能影响吞吐量——仅作为临时 workaround，长期应调整 TP 值。

## 四、根因二：PP 流水线并行的层分配不对称

### 问题现象

使用 PP=2 部署时，启动日志中两个 worker 的 KV Cache 容量差距悬殊：

```
GPU KV cache size: 46,800 tokens (Worker 0)
GPU KV cache size: 327,680 tokens (Worker 1)
```

两张卡的 KV Cache 容量差了 **7 倍**，显存占用差距可达 4~8 GB。Worker 0 的 KV Cache 池过小，成为系统瓶颈——所有请求的实际可用 KV Cache 空间受限于最小的那张卡。

### 根因分析

当使用流水线并行（PP）时，模型的不同层被分配到不同 GPU 上。如果层分配不对称——例如一个 64 层的模型按 PP=2 分配为 56 层 vs 8 层（由于某些层在特定 stage 的计算量更大，调度器可能做出不均匀分配）——各 worker 的 KV Cache 张量大小就会不一致。

更隐蔽的浪费机制是：vLLM 内部以 KV Cache 容量最小的 worker 为准确定整个系统的可用 token 上限（否则最小容量的卡会先溢出），但各 worker 的 KV Cache 张量仍然按其自身计算的容量来分配显存。结果就是容量最大的 worker 分配了大量显存给 KV Cache，但实际可用的 token 数却被最小 worker 锁定——**大卡的 KV Cache 池大量空间被预留但永远不会被使用**。

此外，如果混用不同显存大小的 GPU（如 16 GB + 32 GB），vLLM 以最小显存卡为基准规划 KV Cache 池，大显存卡的大量剩余空间被浪费。

### 社区 Issue 参考

以下为 vLLM GitHub 上与 KV Cache 分配不均相关的真实 Issue：

| Issue | 问题描述 | 状态 |
|:---|:---|:---|
| [#25164](https://github.com/vllm-project/vllm/issues/25164) | PP 下 KV Cache 分配不对称 | 已修复（v0.10.2+） |
| [#34076](https://github.com/vllm-project/vllm/issues/34076) | PP 下 KV Cache 容量计算偏差 | 讨论中 |
| [#11797](https://github.com/vllm-project/vllm/issues/11797) | 非整除 attention heads 的 padding 支持 | 已支持 |

### 解决方案

**方案一**：使用 `--kv-cache-memory-bytes` 手动指定 KV Cache 大小。

```bash
vllm serve /path/to/model \
    --pipeline-parallel-size 2 \
    --kv-cache-memory-bytes 5G
```

强制 vLLM 按指定大小分配 KV Cache 池，避免因不对称计算导致的超大预留。此方案适合明确知道负载所需 KV Cache 量的场景。

**方案二**：升级 vLLM 到 0.10.2 以上版本。

```bash
pip install --upgrade vllm>=0.10.2
```

PR #25164 已在 0.10.2 中修复了 PP 下 KV Cache 分配不对称的问题。

**方案三**：手动控制各 PP stage 的层分配，使层数尽量均衡。

```bash
VLLM_PP_LAYER_PARTITION=32,32 vllm serve /path/to/model --pipeline-parallel-size 2
```

此环境变量允许显式指定每个 PP stage 负责的层数，从根源上消除层分配不均。

## 五、四步排查法

当怀疑存在 KV Cache 分配不均时，按以下顺序排查：

**Step 1：检查启动日志中的 KV Cache 分配信息**

```bash
vllm serve /path/to/model --tensor-parallel-size 2 2>&1 | grep -i "kv cache"
```

关注各 worker 的 `GPU KV cache size` 行。如果不同 worker 的数值差异超过 10%，说明存在分配不均。

**Step 2：检查模型 config 中的 head 数和层数**

```bash
cat /path/to/model/config.json | grep -E "num_attention_heads|num_key_value_heads|num_hidden_layers"
```

验证两个关键约束：
- `num_attention_heads % tensor_parallel_size == 0`
- `num_key_value_heads % tensor_parallel_size == 0`

**Step 3：使用 nvidia-smi 持续监控显存漂移**

```bash
watch -n 1 nvidia-smi
```

观察各卡显存占用是否持续扩大差距。短期运行后差距超过 2 GB 即需排查。

**Step 4：检查 vLLM metrics 端点中的 KV Cache 使用率**

```bash
curl http://localhost:8000/metrics | grep kv_cache
```

重点关注 `vllm:kv_cache_usage_perc`——各卡的 KV Cache 实际使用率。如果不同 GPU 的数值差异超过 15%，几乎可以确认存在分配不均问题。

## 六、总结与面试金句

### 两大根因速查

| 根因 | 触发条件 | 核心解决方案 |
|:---|:---|:---|
| head 数无法被 TP 整除 | `num_heads % tp_size != 0` | 选择可整除的 TP 值（查 config.json） |
| PP 下层分配不对称 | PP≥2 时各 stage 层数不均 | `--kv-cache-memory-bytes` 或升级 vLLM ≥ 0.10.2 |

### 面试场景速记

> 在多卡 vLLM 部署中，KV Cache 显存分配不均是常见但容易被忽视的问题。我梳理过两种典型场景：
>
> 第一，注意力头数无法被 TP 整除。比如 Qwen2-7B 有 28 个 attention heads，TP=8 时 28 无法被 8 整除，vLLM 会直接报错或通过 padding 导致各卡 KV Cache 大小不一。解决方案是检查模型 config，选择能被整除的 TP 值——Qwen2-7B 推荐 TP=2 或 4。
>
> 第二，PP 流水线并行下的层分配不对称。比如 64 层模型 PP=2 时分配为 56 层 vs 8 层，Worker 0 的 KV Cache 只有 46,800 tokens 而 Worker 1 高达 327,680 tokens——但实际可用容量被最小卡锁定，大卡的多余分配完全浪费。解决方案是使用 `--kv-cache-memory-bytes` 手动控制 KV Cache 大小，或升级到 vLLM 0.10.2 以上版本。
>
> 排查时我的 SOP 是：先看启动日志中各 worker 的 GPU KV cache size 是否一致，再检查模型 config 确认 head 数能否整除 TP，最后用 `nvidia-smi` 和 `/metrics` 端点持续监控各卡显存差异。
>
> 在我实际部署的 Qwen2.5-7B（28 heads）+ TP=2 环境中，28 ÷ 2 = 14 完美整除，双卡显存持续一周稳定在 74003 MiB/74003 MiB——选择正确的 TP 值即可从根本上避免这个问题。

## 七、附录：测试环境

| 项目 | 配置 |
|:---|:---|
| GPU | 2 × NVIDIA A100 80GB PCIe（无 NVLink，SYS 拓扑） |
| 模型 | Qwen/Qwen2.5-7B-Instruct（28 attention heads） |
| vLLM | 0.19.0 |
| TP | 2（28 ÷ 2 = 14，完美整除） |
| 运行时长 | 持续运行 1 周，双卡显存始终对称 |
| 操作系统 | Ubuntu 24.04 |

---

*测试环境：双 A100 80GB PCIe（无 NVLink）· vLLM 0.19.0 · Qwen2.5-7B-Instruct · TP=2 · 2026 年 7 月*
